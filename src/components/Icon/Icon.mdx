import { ArgTypes, Canvas, Meta, Primary, Title } from '@storybook/blocks';
import * as IconStories from './Icon.stories';

<Meta of={IconStories} />

<Title />

## Overview

- Decorative and semantic icons in buttons, lists, and empty states
- Standardized MDI glyphs from the design system icon registry
- Token-driven sizes and semantic icon colors

## Implementation notes

- Registry in `icons.ts` — regenerate with `npm run generate-icons` after Figma updates
- Custom icons live in `custom.ts` when needed
- **Gallery** below lists all icons from Figma frame Icons (12663:7261)

## Props

<ArgTypes of={IconStories} />

## Preview

<Primary />

## Gallery

<Canvas of={IconStories.Gallery} />
