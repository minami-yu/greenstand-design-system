import { ArgTypes, Canvas, Meta } from '@storybook/blocks';
import * as IconStories from './Icon.stories';

<Meta of={IconStories} />

# Icon

## Overview
Component to display icons. Please check usage and details on the Foundation/Iconography page.

<br />
<Canvas of={IconStories.Playground} />

## Props

<ArgTypes of={IconStories} />


## Implementation
- Registry in `icons.ts` — regenerate with `npm run generate-icons` after Figma updates
- Custom icons live in `custom.ts` when needed

{/* ## Gallery
Check the Foundation/Iconography page for datail.
<Canvas of={IconStories.Gallery} /> */}

## Accessibility
Pass `accessibilityLabel` when the icon conveys meaning (close, delete, status). Omit for decorative icons inside labeled controls. See **Accessibility/Overview**.
