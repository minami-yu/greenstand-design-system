import { ArgTypes, Canvas, Meta } from '@storybook/blocks';
import * as ProgressBarStories from './ProgressBar.stories';

<Meta of={ProgressBarStories} />

# ProgressBar
## Overview
Horizontal progress indicator for task completion and loading states.

<br />
<Canvas of={ProgressBarStories.Playground} />

## Props
<ArgTypes of={ProgressBarStories} />

## Variants

### Progress values
<Canvas of={ProgressBarStories.Variants} />
