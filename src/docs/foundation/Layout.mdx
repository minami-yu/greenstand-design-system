import { Meta } from '@storybook/blocks';
import { LayoutExample } from './partials';
import { DocBulletList, DocCallout, DocCode, MdxDocPage, DocSection } from '../ui';

export const parameters = {
  controls: { disable: true },
  docsPage: true
};

<Meta title="Foundation/Layout" parameters={parameters} />

<MdxDocPage
  description="Layout primitives and spacing conventions for composing screens."
  title="Layout"
>
  <DocSection title="Layout tokens">
    <DocBulletList
      items={[
        'Use theme.space for padding, margin, and gap values',
        'Use theme.radius for corner radius values',
        'Prefer shared tokens instead of arbitrary layout numbers'
      ]}
    />
    <DocCode code={`<View style={{ padding: theme.space['400'], gap: theme.space['200'], borderRadius: theme.radius.md }}>\n  {children}\n</View>`} />
    <LayoutExample />
  </DocSection>

  <DocCallout text="See Foundation/Spacing for the full space scale." />
</MdxDocPage>
