import { Meta } from '@storybook/blocks';
import { RadiusCatalog } from './catalogs';
import { MdxDocPage } from '../ui';
import { buildScaleCatalogEntries } from '../utils/buildCatalogEntries';

export const radius = buildScaleCatalogEntries('radius', 'radius');
export const parameters = {
  controls: { disable: true },
  docsPage: true
};

<Meta title="Foundation/Radius" parameters={parameters} />

<MdxDocPage
  title="Radius"
>
  <RadiusCatalog entries={radius} />
</MdxDocPage>
