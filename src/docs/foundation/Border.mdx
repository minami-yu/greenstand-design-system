import { Meta } from '@storybook/blocks';
import { MdxDocPage, TokenCatalog } from '../ui';
import { buildScaleCatalogEntries } from '../utils/buildCatalogEntries';

export const borders = buildScaleCatalogEntries('stroke', 'stroke');
export const parameters = {
  controls: { disable: true },
  docsPage: true
};

<Meta title="Foundation/Border" parameters={parameters} />

<MdxDocPage
  title="Border"
>
  <TokenCatalog entries={borders} />
</MdxDocPage>
