import { Meta } from '@storybook/blocks';
import { ColorCatalog } from './catalogs';
import { MdxDocPage } from '../ui';
import { buildColorCatalogEntries } from '../utils/buildCatalogEntries';

export const colors = buildColorCatalogEntries();
export const parameters = {
  controls: { disable: true },
  docsPage: true
};

<Meta title="Foundation/Colors" parameters={parameters} />

<MdxDocPage
  title="Colors"
>
  <ColorCatalog entries={colors} />
</MdxDocPage>
