import { Meta } from '@storybook/blocks';
import { TypographyCatalog, TypographyUsageTable } from './catalogs';
import { DocSection, MdxDocPage, sbTypography } from '../ui';
import { buildTypographyCatalogEntries } from '../utils/buildCatalogEntries';
import { Text } from 'react-native';
import { useTheme } from '../../theme/useTheme';

export const typography = buildTypographyCatalogEntries();
export const parameters = {
  controls: { disable: true },
  docsPage: true
};

export function OverviewCopy() {
  const t = useTheme();

  return (
    <Text style={[sbTypography['paragraph-l'], { color: t.color.text.neutral.secondary }]}>
      Composite text styles from tokens/styles/value.json — spread into React Native Text styles.
    </Text>
  );
}

<Meta title="Foundation/Typography" parameters={parameters} />

<MdxDocPage title="Typography">
  <DocSection title="Overview">
    <OverviewCopy />
  </DocSection>

  <DocSection title="Preview">
    <TypographyCatalog entries={typography} />
  </DocSection>

  <DocSection title="Usage">
    <TypographyUsageTable entries={typography} />
  </DocSection>
</MdxDocPage>
