import { Meta } from '@storybook/blocks';
import { RadiusCatalog } from './sections';
import { buildScaleCatalogEntries } from '../utils/buildCatalogEntries';
import { storybookPageParameters as parameters } from '../storybookPageParameters';

export const radius = buildScaleCatalogEntries('radius', 'radius');

<Meta title="Foundation/Radius" parameters={parameters} />

# Radius
## Overview
Radius tokens define the corner roundness of components. Using predefined radius values helps maintain visual consistency across components and layouts.

## Tokens
Use the following tokens to set the corner roundness of components.
<RadiusCatalog entries={radius} />
